import { StyleSheet } from "react-native";
import Colors from "./Colors";

export default StyleSheet.create({
    MainBtn: {
        backgroundColor: Colors.MAIN_COLOR,
        color: Colors.WIHTE_COLOR
    }
});
